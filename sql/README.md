# SQL-Hilfsskripte

| Datei | Zweck |
|--------|--------|
| `add-profiles-dashboard-layout.sql` | Spalte `profiles.dashboard_layout` für synchrones Dashboard-Layout (Haupt-App). |

**Ausführung (Supabase):** Dashboard → **SQL Editor** → Inhalt der Datei einfügen → **Run**.

Die vollständige Schema-Referenz liegt in `supabase-complete.sql` im Projektroot.
