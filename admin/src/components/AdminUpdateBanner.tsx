import UpdateBanner from '../../../shared/UpdateBanner'

/**
 * Gleiches Verhalten wie in den Mandanten-Apps: Hinweis nur, wenn `version.json` neuer ist als der Build.
 */
const AdminUpdateBanner = () => <UpdateBanner />

export default AdminUpdateBanner
